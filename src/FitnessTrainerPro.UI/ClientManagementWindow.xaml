<Window x:Class="FitnessTrainerPro.UI.ClientManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FitnessTrainerPro.UI"
        mc:Ignorable="d"
        Title="Управление клиентами" Height="450" Width="800"
        WindowStartupLocation="CenterOwner" Loaded="Window_Loaded">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Name="AddClientButton" Content="Добавить клиента" Width="150" Margin="5" Click="AddClientButton_Click"/>
            <Button Name="EditClientButton" Content="Редактировать" Width="120" Margin="5" Click="EditClientButton_Click"/>
            <Button Name="DeleteClientButton" Content="Удалить клиента" Width="150" Margin="5" Click="DeleteClientButton_Click"/>
        </StackPanel>

        <ListView Name="ClientsListView" DockPanel.Dock="Top">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding ClientID}" Width="50"/>
                    <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding FirstName}" Width="200"/>
                    <GridViewColumn Header="Фамилия" DisplayMemberBinding="{Binding LastName}" Width="200"/>
                    <GridViewColumn Header="Дата рождения" DisplayMemberBinding="{Binding DateOfBirth, StringFormat='dd.MM.yyyy'}" Width="150"/>
                    <!-- Добавим другие поля позже -->
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>